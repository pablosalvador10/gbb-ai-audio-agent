// Simple test to verify React import works
import React from 'react';
import { createRoot } from 'react-dom/client';

console.log('React version:', React.version);
console.log('createRoot function:', typeof createRoot);
console.log('✅ React imports working correctly!');
